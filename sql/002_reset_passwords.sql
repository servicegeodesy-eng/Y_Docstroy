-- ============================================================================
-- Сброс паролей всех пользователей на временный 'changeme123'
-- Выполни в Yandex Cloud SQL консоли
-- ============================================================================

UPDATE users
SET password_hash = crypt('changeme123', gen_salt('bf', 12)),
    must_change_password = true;
